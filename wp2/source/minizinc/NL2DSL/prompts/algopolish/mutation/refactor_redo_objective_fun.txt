[Code]
```
{}
```

[Objective_approaches]
{}

[Task]
Perform the following three steps:
1. Remove the code within section "# --- Objective ---" in [Code] completely.
2. Encode exactly the full description of the {}. approach from [Objective_approaches] as valid OptDSL formulation into section "# --- Objective ---" in [Code]. Update the assignment to "objective" variable accordingly. Update affected decision variable constraints accordingly. Note in a comment the name of the chosen approach. Do not return the old code in section "# --- Objective ---".
3. After refactoring, list each decision variable and enumerate all constraints that apply to it. Confirm that none are missing according to [Problem].

Fulfill the following rules:
- Do not use list comprehension. Do not use any list multiplication operator on arrays. Code like `[1] * 5` and `[True for _ in range(1, 5)]` is illegal. Do not use function calls append() and extend() for DSList. Do not initialize default values.
- Do not use floating-point division. Do not define constants (upper-case variables) within functions. Use lower-case variables instead.
- Do not initialize list-elements of decision variables manually!
- Encode the function in the most efficient way and with minimal necessary number of lines. If constraints remain unchanged, write them again.
- Add a variable "objective", if not existing, and that it is linked to the problem.
- Check that all decision variables from [Decision_variables] are integrated in assert-constraints. Add missing semantic constraints.
- Constrain every decision variable with at least one other decision variable.
- For every decision variable, ensure that:
    - All its original constraints, bounds, and relationships are defined according to [Problem].
    - No variable becomes unconstrained.
    - If constraints are rewritten, they must be logically equivalent
    - every decision variable is linked with at least one other according to [Problem].
- Do not return explanations or markdown code.
- Provide your answer as valid OptDSL code enclosed with codeblock in the following format:
```
# --- Objective ---
<objective_function>
objective = <objective_function>(...)
<optimize>(objective)

<missing_decision_variable_constraint_update>

# --- Constraints ---
<constraints>
```, replace <objective_function> with OptDSL code of the objective function in a view lines, replace <optimize> with minimize or maximize, replace <decision_variable_constraint_update> with all missing assert-constraints for decision variables in [decision variables], according to semantic [Problem]. Replace <constraints> with OptDSL code of the constraints of [Code].

[Decision_variables]
{}

[Problem]
{}
